# make it standalone script which takes parameters from command line

def add(a,b):
    return a+b

def decode(filename, label, columns, shouldBeTransposed, multiplier, outputFolder, outputName):
    print("not yet implemented")

    # encode the data with afm reader

    # get the values corresponding to label
        #if values does not exist then throw

    # if shouldBeTransposed === true then transpose

    # multiply by multiplier

    # choose how many columns

    # save .csv to outputFolder/outputName


